import React from 'react';
import {Row, Col, Flex, Text} from './common/base';
import {componentBger} from '../styles';

export const Footer = ({}) => {
    return (
        <Row align="center" justify="center" padding="padding: 50px;" bg={componentBger()} height="300px">
            
        </Row>
    )
}