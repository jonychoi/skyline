import React, {useState, useEffect} from 'react';
import {Flex, Col, Row, Text} from '../../../../components/common/base';

export const Btns = ({}) => {
    return (
        <Row>

         </Row>
    )
};